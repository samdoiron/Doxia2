#!/usr/bin/env ruby -Itest

require_relative '../lib/core'
require 'minitest'

gem_root = Pathname(File.expand_path(File.dirname(__FILE__))).parent
Dir.glob(gem_root.to_s + '/test/*_test.rb').each do |file|
  require gem_root.join(file)
end

Minitest.autorun